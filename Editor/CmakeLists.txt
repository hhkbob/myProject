#*================================C++========================================*\
#      Create: 2018/02/21
#      Creator: Huang Huakun
#      File Name: CFDMainWindow/CmakeLists.txt
#      Platform: windows
#      Version: v1.0
#
#      Describe: Define the tabWidget reaction.
#
#      Example:
#
#*===========================================================================*/

#  The code's length should not exceed 80 characters.
#  Every description for a code line should skip two characters.
#  As you can, do not use the void function. A return code is welcome.

#
#    CopyRight:
#

set( CMAKE_AUTOMOC ON )
set( CMAKE_AUTOUIC ON )
set( CMAKE_AUTORCC ON )

find_package( Qt5Widgets REQUIRED)
find_package( ParaView REQUIRED )
include( ${PARAVIEW_USE_FILE} )

set( SOURCE_FILE TabWidget.cpp )
set( HEADER_FILE TabWidget.h )
qt5_wrap_cpp( moc ${SOURCE_FILE} )
